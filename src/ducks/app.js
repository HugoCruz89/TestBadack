import {createAction, createReducer} from '@reduxjs/toolkit';

// const

//actions

// init state
const initState = {
  profile: null,
};

// reducer
const reducer = createReducer(initState, bulder => {});
export default reducer;
